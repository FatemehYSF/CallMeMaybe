[project]
name = "call-me-maybe"
version = "0.1.0"
description = "Call Me Maybe project"
requires-python = ">=3.10"
dependencies = [
    "llm-sdk",
    "pydantic>=2.13.4",
]

[tool.uv.sources]
llm-sdk = { path = "llm_sdk" }
